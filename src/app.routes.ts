import { Routes } from '@angular/router';
import { DataTableComponent } from './app/components/data-table/data-table.component';

export const routes: Routes = [{ path: '', component: DataTableComponent }];
